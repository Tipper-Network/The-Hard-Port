# THP API — server-side only (Nest backend)
THP_API_URL=http://localhost:3001

# Public API base (reviewer OAuth login links)
NEXT_PUBLIC_THP_API_URL=http://localhost:3001

# Public site URL — metadataBase, OG images
NEXT_PUBLIC_SITE_URL=http://localhost:3000
