# Context trim inventory
# Generated by trim — 2026-07-17 (first real audit for this repo)

version: 1
measured_at: 2026-07-17
budget_target_bytes: 20480
budget_target_note: always-on rules only (AGENTS.md separate; currently absent)

segments:
  - id: 00-brand-core
    path: .cursor/rules/00-brand-core.mdc
    always_apply: true
    bytes: 3605
    est_tokens: 901
    verdict: keep
    mechanism: only always-on rule in the repo; well under budget
    status: keep

  - id: 01-copywriting-standards
    path: .cursor/rules/01-copywriting-standards.mdc
    always_apply: false
    globs: ["**/*.tsx", "**/*.jsx", "**/*.md", "**/*.mdx", "**/content/**", "**/copy/**"]
    bytes: 2748
    verdict: keep
    mechanism: path-scoped — good pattern
    status: keep

  - id: 02-conversion-funnel
    path: .cursor/rules/02-conversion-funnel.mdc
    always_apply: false
    globs: ["**/*.tsx", "**/*.jsx", "**/app/**", "**/pages/**", "**/components/**"]
    bytes: 2741
    verdict: keep
    mechanism: path-scoped
    status: keep

  - id: 03-ui-visual-system
    path: .cursor/rules/03-ui-visual-system.mdc
    always_apply: false
    globs: ["**/*.css", "**/*.scss", "**/*.tsx", "**/*.jsx", "**/tailwind.config.*", "**/components/**", "**/styles/**"]
    bytes: 2103
    verdict: keep
    mechanism: path-scoped
    status: keep

  - id: 04-ux-friction
    path: .cursor/rules/04-ux-friction.mdc
    always_apply: false
    globs: ["**/*.tsx", "**/*.jsx", "**/app/**", "**/pages/**", "**/forms/**"]
    bytes: 1894
    verdict: keep
    mechanism: path-scoped
    status: keep

  - id: agents-md
    path: AGENTS.md
    always_apply: n/a
    bytes: 0
    verdict: n/a
    mechanism: file does not exist yet — no entry-pointer tax to trim
    status: n/a

  - id: instincts-total
    path: .cursor/instincts/*/SKILL.md
    always_apply: false
    bytes: 12164
    verdict: keep
    mechanism: invoked on relevant context, not injected every turn; largest two are aggression-calibration (2402) and small-business-psychology (2372), both justified by breadth of guidance
    status: keep

  - id: skills-total
    path: .cursor/skills/*/SKILL.md
    always_apply: false
    bytes: 15947
    verdict: keep
    mechanism: invoked by name for a specific job, not injected every turn
    status: keep

  - id: mcp-global
    path: ~/.cursor/mcp.json
    always_apply: n/a
    verdict: ask
    mechanism: audit which MCP servers are actually used for Hard Port work; disable unused ones per developer machine
    status: pending
    note: not in repo — manual, per developer

summary:
  always_on_rules_bytes: 3605
  always_on_est_tokens: 901
  scoped_rules_bytes: 9486
  instincts_bytes: 12164
  skills_bytes: 15947
  agents_md_bytes: 0
  per_turn_estimate_bytes: 3605
  per_turn_estimate_tokens: 901
  budget_target_bytes: 20480
  budget_headroom_bytes: 16875
  note: "Repo is well under budget — 00-brand-core.mdc is the only always-on rule. No trim action needed at this size. Re-audit if a second alwaysApply:true rule is added or per-turn exceeds ~15KB."
